clc;
clear;

L = input("Enter span length (m) = ");
T = input("Enter tension (kg) = ");

// Weight range
W = 0.75:0.05:1;

// Sag calculation
S = (W .* L^2) / (8 * T);

// Plot
plot(W, S);
xlabel("Weight of conductor (kg)");
ylabel("Sag (m)");
title("Calculation of Sag with Various Conductor Weights");

// Display sag values
disp("Sag values (m) = " + string(S));
